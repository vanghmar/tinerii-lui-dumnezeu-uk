import type { GalleryImage } from "./types";

// Add photos here ONLY after the consent process in the README has been followed.
// Files go in public/gallery/ — strip EXIF first: exiftool -all= photo.jpg
export const gallery: GalleryImage[] = [];
